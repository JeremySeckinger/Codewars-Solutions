const multiply = (num1, num2) => {return num1 * num2};

//could have made it simpler like this:

//let multiply = (a, b) => a * b;

//or added checks for num:

// function multiply(a, b){
//     if(typeof a == 'number' && typeof b == 'number')
//       return a * b;  
//   }